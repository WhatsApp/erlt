
-module(undefined_remote_constr).

-export([f/1]).

-export_type([foo/0])

-enum foo() :: (bar).

%% Error: referring to an unknown constructor in an existing enum
f(?MODULE:foo.baz) ->
    true.

-spec f(_A) -> boolean().
