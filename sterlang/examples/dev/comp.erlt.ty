-spec map(fun((A) -> B), list(A)) -> list(B).
-spec filter(fun((A) -> boolean()), list(A)) -> list(A).
-spec strange_perm(fun((A, B) -> boolean()), list(A), list(B)) -> list({A, B}).
-spec sort(list(A)) -> list(A).
-spec sort(list(A), fun((A, A) -> boolean())) -> list(A).
-spec append(list(list(A))) -> list(A).
-spec flat_map(fun((A) -> list(B)), list(A)) -> list(B).
-spec list_from_binary(binary()) -> list(integer()).
-spec binary_from_list1(list(integer())) -> binary().
-spec binary_from_list2(list(binary())) -> binary().
-spec even_from_binary(binary()) -> list(integer()).
-spec rgb(binary()) -> list({integer(), integer(), integer()}).
-spec pixels(list({integer(), integer(), integer()})) -> binary().
-spec slice(binary(), integer()) -> list(binary()).
