-spec empty() -> map_ffi:map(A, B).
-spec get(A, map_ffi:map(A, B)) -> maybe:maybe(B).
-spec member(A, map_ffi:map(A, B)) -> boolean().
-spec size(map_ffi:map(A, B)) -> number().
-spec is_empty(map_ffi:map(A, B)) -> boolean().
-spec insert(A, B, map_ffi:map(A, B)) -> map_ffi:map(A, B).
-spec remove(A, map_ffi:map(A, B)) -> map_ffi:map(A, B).
-spec update(A, fun((maybe:maybe(B)) -> maybe:maybe(B)), map_ffi:map(A, B)) -> map_ffi:map(A, B).
-spec singleton(A, B) -> map_ffi:map(A, B).
-spec union(map_ffi:map(A, B), map_ffi:map(A, B)) -> map_ffi:map(A, B).
-spec intersect(map_ffi:map(A, B), map_ffi:map(A, B)) -> map_ffi:map(A, B).
-spec diff(map_ffi:map(A, B), map_ffi:map(A, B)) -> map_ffi:map(A, B).
-spec fold(fun((A, B, C) -> C), C, map_ffi:map(A, B)) -> C.
-spec map(fun((A, B) -> C), map_ffi:map(A, B)) -> map_ffi:map(A, C).
-spec filter(fun((A, B) -> boolean()), map_ffi:map(A, B)) -> map_ffi:map(A, B).
-spec partition(fun((A, B) -> boolean()), map_ffi:map(A, B)) -> {map_ffi:map(A, B), map_ffi:map(A, B)}.
-spec keys(map_ffi:map(A, B)) -> list(A).
-spec values(map_ffi:map(A, B)) -> list(B).
-spec to_list(map_ffi:map(A, B)) -> list({A, B}).
-spec from_list(list({A, B})) -> map_ffi:map(A, B).
