{deps, [
    {erltc, {path, "../../../erltc"}}
]}.

{escript_incl_apps, [rpctc]}.

{escript_main_app, rpctc}.

{escript_name, rpctc}.

{erlc_compiler, [{recursive, false}]}.

{provider_hooks, [{post, [{compile, escriptize}]}]}.

{plugins, [
    rebar3_path_deps
]}.
