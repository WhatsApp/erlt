{deps, []}.

{escript_incl_apps, [erlt]}.

{escript_main_app, erlt}.

{escript_name, erlt}.

{provider_hooks, [{post, [{compile, escriptize}]}]}.

% creates the makefile used for incremental builds
{pre_hooks, [
    {compile, "escript make_erlt_build_template_mk.escript"}
]}.
