{deps, [erltc]}.

{escript_incl_apps, [erlbuild]}.

{escript_main_app, erlbuild}.

{escript_name, erlbuild}.

{provider_hooks, [{post, [{compile, escriptize}]}]}.

{pre_hooks, [
    {compile, "escript make_erlbuild_template_mk.escript"}
]}.
