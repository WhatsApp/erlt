-file("import_enum.erl", 1).

-module(import_enum).

-compile([warn_unused_import, warnings_as_errors]).

-export([p/2]).

-import_type({mod03, [{pair, 2}]}).

-spec p(A, B) -> mod03:pair(A, B).

p(A, B) ->
    {969696, import_enum, pair, pair, A, B}.



