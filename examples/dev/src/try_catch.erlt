-lang([erlt]).
-module(try_catch).

-export([t1/2]).

t1(F, Class) ->
    try
        F()
    catch
        ^Class, Reason, Stack -> {Reason, Stack};
        exit, Reason, _ -> Reason;
        error, _ -> error;
        throw, Reason -> Reason
    end.
