% this is a temporary workaround for a make problem (at least on mac). For some obscure
% reason, rebuild of the first source in the list of sources doesn't trigger rebuild of
% escripts.
-module(a0).
