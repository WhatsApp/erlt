# Code generated by dhall-to-yaml.  DO NOT EDIT.
jobs:
  checkEqwalizer:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: "actions/checkout@v2"
      - name: Set up Java
        uses: "actions/setup-java@v1"
        with:
          java-version: '11'
      - name: Coursier Cache
        uses: "coursier/cache-action@v3"
        with:
          root: eqwalizer
      - name: Erlang version
        run: "erl -eval 'erlang:display(erlang:system_info(otp_release)), halt().'  -noshell"
      - name: test eqwalizer
        run: "cd eqwalizer && sbt 'coverage;test;coverageReport;scalafmtCheckAll'"
name: Eqwalizer CI
on:
  push:
    paths:
      - "eqwalizer/**"
      - ".github/**"
