# Code generated by dhall-to-yaml.  DO NOT EDIT.
jobs:
  buildJar:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: "actions/checkout@v2"
      - name: Set up Java
        uses: "actions/setup-java@v1"
        with:
          java-version: '11'
      - name: Coursier Cache
        uses: "coursier/cache-action@v3"
        with:
          root: sterlang
      - name: assembly sterlang.jar
        run: "cd sterlang; sbt assembly"
      - name: upload sterlang.jar
        uses: "actions/upload-artifact@v2"
        with:
          name: sterlang.jar
          path: sterlang/target/scala-2.13/sterlang.jar
          retention-days: '8'
  linuxBinary:
    needs:
      - buildJar
    runs-on: ubuntu-latest
    steps:
      - name: Set up Java
        uses: "actions/setup-java@v1"
        with:
          java-version: '11'
      - name: Setup GraalVM Environment
        uses: "DeLaGuardo/setup-graalvm@master"
        with:
          graalvm-version: "20.2.0.java11"
      - name: Install Native Image Plugin
        run: gu install native-image
      - name: Get JAR Artifact
        uses: "actions/download-artifact@v2"
        with:
          name: sterlang.jar
      - name: ls
        run: ls -lah
      - name: "Build native image 'sterlang-linux-draft'"
        run: native-image -R:MaxHeapSize=16m --no-server --no-fallback -jar sterlang.jar sterlang-linux-draft
      - name: upload artifacts
        uses: "actions/upload-artifact@v2"
        with:
          name: sterlang-linux-draft
          path: sterlang-linux-draft
          retention-days: '8'
  macBinary:
    needs:
      - buildJar
    runs-on: macos-latest
    steps:
      - name: Set up Java
        uses: "actions/setup-java@v1"
        with:
          java-version: '11'
      - name: Setup GraalVM Environment
        uses: "DeLaGuardo/setup-graalvm@master"
        with:
          graalvm-version: "20.2.0.java11"
      - name: Install Native Image Plugin
        run: gu install native-image
      - name: Get JAR Artifact
        uses: "actions/download-artifact@v2"
        with:
          name: sterlang.jar
      - name: ls
        run: ls -lah
      - name: "Build native image 'sterlang-mac-draft'"
        run: native-image -R:MaxHeapSize=16m --no-server --no-fallback -jar sterlang.jar sterlang-mac-draft
      - name: upload artifacts
        uses: "actions/upload-artifact@v2"
        with:
          name: sterlang-mac-draft
          path: sterlang-mac-draft
          retention-days: '8'
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: "actions/checkout@v2"
      - name: Set up Java
        uses: "actions/setup-java@v1"
        with:
          java-version: '11'
      - name: Coursier Cache
        uses: "coursier/cache-action@v3"
        with:
          root: sterlang
      - name: Erlang version
        run: "erl -eval 'erlang:display(erlang:system_info(otp_release)), halt().'  -noshell"
      - name: assemble erltc
        run: rebar3 escriptize
      - name: compile stErlang
        run: "cd sterlang; sbt --client compile"
      - name: test stErlang
        run: "cd sterlang; sbt --client test"
      - env:
          ERL_FLAGS: "-args_file dev.vm.args"
        name: "test erltc + stErlang in dev mode"
        run: |
          cd sterlang
          sbt --client 'test:bgRunMain com.whatsapp.sterlang.dev.SterlangD'
          cd examples
          rebar3 compile
name: Sterlang CI
on:
  push:
    paths:
      - "sterlang/**"
      - ".github/**"
