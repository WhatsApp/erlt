# Code generated by dhall-to-yaml.  DO NOT EDIT.
jobs:
  buildJar:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: "actions/checkout@v2"
      - name: Set up Java
        uses: "actions/setup-java@v1"
        with:
          java-version: '11'
      - name: Coursier Cache
        uses: "coursier/cache-action@v3"
        with:
          root: sterlang
      - name: assembly sterlang.jar
        run: "cd sterlang; sbt assembly"
      - name: upload sterlang.jar
        uses: "actions/upload-artifact@v2"
        with:
          name: sterlang.jar
          path: sterlang/target/scala-2.13/sterlang.jar
          retention-days: '8'
  linuxBinary:
    needs:
      - buildJar
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: "actions/checkout@v2"
      - name: Set up Java
        uses: "actions/setup-java@v1"
        with:
          java-version: '11'
      - name: Setup GraalVM Environment
        uses: "DeLaGuardo/setup-graalvm@master"
        with:
          graalvm-version: "20.1.0.java11"
      - name: Install Native Image Plugin
        run: gu install native-image
      - name: Get JAR Artifact
        uses: "actions/download-artifact@v2"
        with:
          name: sterlang.jar
      - name: ls
        run: ls -lah
      - name: "Build native image 'sterlang-linux'"
        run: native-image -R:MaxHeapSize=16m --no-server --no-fallback -jar sterlang.jar sterlang-linux
      - name: "if erlang-less, assume we're on mac and brew install"
        run: "which erl || brew install erlang"
      - name: upload artifacts
        uses: "actions/upload-artifact@v2"
        with:
          name: sterlang-linux
          path: sterlang-linux
          retention-days: '8'
      - name: test erltc with native image
        run: "mkdir -p erltc/priv && cp sterlang-linux erltc/priv/sterlang-linux && make -C tests test-native"
  macBinary:
    needs:
      - buildJar
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: "actions/checkout@v2"
      - name: Set up Java
        uses: "actions/setup-java@v1"
        with:
          java-version: '11'
      - name: Setup GraalVM Environment
        uses: "DeLaGuardo/setup-graalvm@master"
        with:
          graalvm-version: "20.1.0.java11"
      - name: Install Native Image Plugin
        run: gu install native-image
      - name: Get JAR Artifact
        uses: "actions/download-artifact@v2"
        with:
          name: sterlang.jar
      - name: ls
        run: ls -lah
      - name: "Build native image 'sterlang-darwin'"
        run: native-image -R:MaxHeapSize=16m --no-server --no-fallback -jar sterlang.jar sterlang-darwin
      - name: "if erlang-less, assume we're on mac and brew install"
        run: "which erl || brew install erlang"
      - name: upload artifacts
        uses: "actions/upload-artifact@v2"
        with:
          name: sterlang-darwin
          path: sterlang-darwin
          retention-days: '8'
      - name: test erltc with native image
        run: "mkdir -p erltc/priv && cp sterlang-darwin erltc/priv/sterlang-darwin && make -C tests test-native"
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: "actions/checkout@v2"
      - name: Set up Java
        uses: "actions/setup-java@v1"
        with:
          java-version: '11'
      - name: Coursier Cache
        uses: "coursier/cache-action@v3"
        with:
          root: sterlang
      - name: Erlang version
        run: "erl -eval 'erlang:display(erlang:system_info(otp_release)), halt().'  -noshell"
      - name: assemble erltc
        run: rebar3 escriptize
      - name: test stErlang
        run: "cd sterlang && sbt 'coverage;test'"
      - name: "test erltc + stErlang in dev mode"
        run: make -C sterlang/examples build-jar
      - name: coverageReport
        run: "cd sterlang && sbt 'coverageReport'"
name: Sterlang CI
on:
  push:
    paths:
      - "sterlang/**"
      - ".github/**"
