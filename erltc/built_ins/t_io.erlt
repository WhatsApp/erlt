-module(t_io).

-export([format/1, format/2]).


-spec format(string()) -> atom().
[unchecked]
format(Fmt) ->
    io:format(Fmt).

-spec format(string(), _) -> atom().
[unchecked]
format(Fmt, Args) ->
    io:format(Fmt, tuple_to_list(Args)).
